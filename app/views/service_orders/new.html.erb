<h1>Hello, you can create order here</h1>
<%= form_with model: [@user, @user.service_orders.build] do |f|%>
  <%= f.text_field :description%>
  <%= f.submit 'add order'%>
<% end %><br/>
<%= link_to 'all orders', user_service_orders_path(@user) %>